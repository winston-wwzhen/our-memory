/* miniprogram/pages/avatar_detail/index.wxss */
page {
  background: #fff0f5;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', sans-serif;
  height: 100vh;
  overflow: hidden;
  --primary-color: #ff6b81;
  --text-main: #5d4037;
}

.page-container {
  height: 100vh;
  display: flex;
  flex-direction: column;
  position: relative;
}

/* === 🌸 背景装饰 === */
.bg-decoration { position: absolute; width: 100%; height: 100%; top: 0; left: 0; pointer-events: none; z-index: 0; overflow: hidden; }
.floating-bubble { position: absolute; border-radius: 50%; background: radial-gradient(circle, #ffdee9 0%, transparent 70%); opacity: 0.5; animation: float 8s infinite ease-in-out; }
.b1 { width: 400rpx; height: 400rpx; top: -80rpx; left: -100rpx; }
.b2 { width: 300rpx; height: 300rpx; bottom: 20%; right: -50rpx; animation-delay: 2s; background: radial-gradient(circle, #e1bee7 0%, transparent 70%); }
.b3 { width: 120rpx; height: 120rpx; top: 40%; left: 10%; animation-delay: 4s; }
@keyframes float { 0%, 100% { transform: translateY(0) scale(1); } 50% { transform: translateY(-30rpx) scale(1.05); } }

/* === 💎 通用组件 === */
.glass-panel {
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(16px);
  border: 2rpx solid rgba(255, 255, 255, 0.8);
  box-shadow: 0 16rpx 40rpx rgba(255, 182, 193, 0.15);
  border-radius: 32rpx;
}
.glass-panel-sm {
  background: rgba(255, 255, 255, 0.5);
  border: 1rpx solid rgba(255, 255, 255, 0.6);
  border-radius: 24rpx;
}
.slide-down { animation: slideDown 0.6s cubic-bezier(0.25, 1, 0.5, 1); }
.fade-in { animation: fadeIn 0.8s ease-out; }
@keyframes slideDown { from { transform: translateY(-40rpx); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

/* === 📜 滚动区域 === */
.main-scroll { flex: 1; width: 100%; position: relative; z-index: 10; }
.scroll-content { padding: 24rpx 32rpx; display: flex; flex-direction: column; gap: 32rpx; }

/* 1. 展示卡片 */
.showcase-card { padding: 30rpx; margin-top: 10rpx; }
.card-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30rpx; }
.title-text { font-size: 32rpx; font-weight: 800; color: var(--text-main); }
.hot-badge { font-size: 20rpx; color: var(--primary-color); background: #fff; padding: 4rpx 12rpx; border-radius: 20rpx; font-weight: bold; box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.05); }

.avatar-pair-box { display: flex; justify-content: space-between; align-items: center; padding: 10rpx 10rpx; }
.avatar-wrap { position: relative; width: 220rpx; height: 220rpx; }
.avatar-img { width: 100%; height: 100%; border-radius: 32rpx; border: 6rpx solid #fff; box-shadow: 0 12rpx 24rpx rgba(139, 69, 19, 0.08); }
.tag-pill {
  position: absolute; bottom: -14rpx; left: 50%; transform: translateX(-50%);
  font-size: 20rpx; font-weight: bold; color: #fff; padding: 4rpx 16rpx; border-radius: 20rpx;
  box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.1); z-index: 2;
}
.boy { background: linear-gradient(90deg, #64b5f6, #42a5f5); }
.girl { background: linear-gradient(90deg, #ff80ab, #ec407a); }

.connector { display: flex; align-items: center; justify-content: center; width: 60rpx; }
.heart-pulse { font-size: 36rpx; color: var(--primary-color); animation: beat 1.2s infinite; text-shadow: 0 0 10rpx rgba(255,107,129,0.4); }
@keyframes beat { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.3); } }

/* 2. 预览区 */
.section-header { margin-bottom: 20rpx; margin-left: 8rpx; }
.section-title { font-size: 26rpx; font-weight: bold; color: #8d6e63; opacity: 0.8; }
.chat-mock-container {
  background: rgba(255, 255, 255, 0.45); border-radius: 32rpx; padding: 30rpx;
  display: flex; flex-direction: column; gap: 24rpx;
  border: 1rpx solid rgba(255,255,255,0.6);
}
.chat-row { display: flex; align-items: flex-start; gap: 16rpx; }
.chat-row.right { flex-direction: row-reverse; }
.chat-avatar { width: 72rpx; height: 72rpx; border-radius: 10rpx; flex-shrink: 0; box-shadow: 0 2rpx 6rpx rgba(0,0,0,0.05); }
.chat-bubble { max-width: 68%; padding: 16rpx 24rpx; font-size: 26rpx; color: #333; line-height: 1.4; position: relative; }
.chat-bubble.left { background: #fff; border-radius: 4rpx 16rpx 16rpx 16rpx; }
.chat-bubble.right { background: #95ec69; border-radius: 16rpx 4rpx 16rpx 16rpx; }

/* 3. 画质选择 */
.quality-card { padding: 20rpx 24rpx; display: flex; align-items: center; justify-content: space-between; background: rgba(255,255,255,0.6); }
.q-label { font-size: 26rpx; font-weight: bold; color: var(--text-main); }
.q-switch-group { display: flex; background: rgba(0,0,0,0.03); border-radius: 36rpx; padding: 4rpx; }
.q-opt { padding: 10rpx 24rpx; border-radius: 30rpx; font-size: 22rpx; color: #999; display: flex; align-items: center; transition: all 0.3s; }
.q-opt.active { background: #fff; color: #333; font-weight: bold; box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.05); }
.lock-tag { font-size: 16rpx; color: var(--primary-color); margin-left: 6rpx; background: rgba(255,107,129,0.1); padding: 2rpx 6rpx; border-radius: 6rpx; }
.vip-tag { font-size: 16rpx; background: #ffd700; color: #fff; margin-left: 6rpx; padding: 2rpx 6rpx; border-radius: 6rpx; }

/* 🚫 垫高底部，防止遮挡 (重要！) */
.safe-area-spacer { height: 300rpx; width: 100%; }

/* === 👇 4. 底部悬浮操作岛 (全新重构) === */
.fixed-footer-island {
  position: fixed;
  bottom: 0; left: 0; width: 100%;
  z-index: 999;
}

.island-bg {
  position: absolute; bottom: 0; left: 0; width: 100%; height: 100%;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(20px);
  box-shadow: 0 -10rpx 40rpx rgba(0,0,0,0.03);
  border-top-left-radius: 40rpx;
  border-top-right-radius: 40rpx;
}

.island-content {
  position: relative;
  /* 🟢 安全区适配：padding-bottom 使用 calc */
  padding: 30rpx 40rpx calc(env(safe-area-inset-bottom) + 30rpx) 40rpx;
  display: flex; align-items: center; justify-content: space-between; gap: 30rpx;
}

/* 左侧：垂直按钮组 */
.action-group-left { display: flex; gap: 32rpx; }
.action-item-vertical { display: flex; flex-direction: column; align-items: center; gap: 8rpx; }

/* 彻底重置 Button 样式 */
.btn-circle-glass {
  width: 90rpx !important; height: 90rpx !important;
  border-radius: 50%;
  background: #fff;
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 6rpx 16rpx rgba(0,0,0,0.05);
  border: 1rpx solid rgba(0,0,0,0.02);
  padding: 0 !important; margin: 0 !important;
  line-height: normal;
  font-weight: normal;
}
/* 去除微信默认边框 */
.btn-circle-glass::after { border: none; }

.emoji-icon { font-size: 40rpx; line-height: 1; margin-bottom: 2rpx; }
.action-txt { font-size: 20rpx; color: #8d6e63; font-weight: 500; }
.btn-hover-pop { transform: scale(0.92); background: #f5f5f5; }

/* 右侧：主按钮 */
.btn-main-pill {
  flex: 1; height: 100rpx;
  background: linear-gradient(135deg, #ff9a9e 0%, #ff6b81 100%);
  border-radius: 50rpx;
  padding: 0 30rpx !important; margin: 0 !important;
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 10rpx 30rpx rgba(255, 107, 129, 0.3);
  border: none;
  line-height: normal;
}
.btn-main-pill::after { border: none; }

.main-icon { font-size: 40rpx; margin-right: 16rpx; color: #fff; }
.main-txt-group { display: flex; flex-direction: column; align-items: flex-start; }
.txt-primary { font-size: 30rpx; font-weight: 800; color: #fff; letter-spacing: 2rpx; line-height: 1.1; }
.txt-sub { font-size: 18rpx; color: rgba(255,255,255,0.9); font-weight: 500; }
.btn-main-hover { transform: scale(0.96); opacity: 0.95; }

.chat-bubble.sticker-bubble {
  background: transparent !important; /* 去掉绿色/白色背景 */
  padding: 0 !important;              /* 去掉内边距 */
  box-shadow: none !important;        /* 去掉阴影 */
  border-radius: 0 !important;
  overflow: visible;
}

/* 🆕 表情包图片样式 */
.chat-sticker {
  width: 180rpx;       /* 稍微大一点，像微信表情包 */
  height: auto;
  display: block;
  filter: drop-shadow(0 4rpx 8rpx rgba(0,0,0,0.1)); /* 给图片加一点投影 */
  
  /* 加个可爱的小动画 */
  animation: popIn 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
}

@keyframes popIn {
  from { transform: scale(0.5); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}