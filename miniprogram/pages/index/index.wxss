/* miniprogram/pages/index/index.wxss */
page {
  background: #fff0f5; 
  font-family: -apple-system, 'Helvetica Neue', sans-serif;
  height: 100vh; 
  overflow: hidden;
}

.journal-container {
  position: relative;
  height: 100%;
  width: 100%;
  background: linear-gradient(180deg, #fff0f5 0%, #fffaf0 100%);
}

.bg-decoration {
  position: absolute;
  top: 0; left: 0; width: 100%; height: 100%;
  pointer-events: none;
  z-index: 0; overflow: hidden;
}
.floating-heart {
  position: absolute;
  bottom: -50rpx;
  font-size: 30rpx; color: #ffdee9; opacity: 0.6;
  animation: floatUp linear infinite;
  text-shadow: 0 0 10rpx rgba(255, 182, 193, 0.5);
}
.h1 { left: 10%; animation-duration: 8s; font-size: 24rpx; }
.h2 { left: 30%; animation-duration: 12s; animation-delay: 2s; font-size: 36rpx; color: #ffb7b2;
}
.h3 { left: 60%; animation-duration: 10s; animation-delay: 4s; }
.h4 { left: 80%; animation-duration: 15s; animation-delay: 1s; font-size: 20rpx;
}
.h5 { left: 90%; animation-duration: 9s; animation-delay: 6s; font-size: 28rpx; }
@keyframes floatUp {
  0% { transform: translateY(0) rotate(0deg);
  opacity: 0; }
  20% { opacity: 0.8; }
  100% { transform: translateY(-120vh) rotate(360deg); opacity: 0;
}
}

.content-wrapper {
  position: relative; z-index: 10;
  display: flex; flex-direction: column; 
  height: 100%;
  padding: 20rpx 40rpx 20rpx 40rpx; 
  box-sizing: border-box;
  justify-content: space-between; 
}

/* === 顶部：任务卡片 === */
.promo-icon { font-size: 40rpx; margin-right: 20rpx; }
.promo-text { flex: 1; display: flex; flex-direction: column; }
.promo-title { font-size: 26rpx;
  font-weight: bold; color: #5d4037; }
.promo-desc { font-size: 22rpx; color: #ff6b81; margin-top: 4rpx; font-weight: bold; }
.promo-arrow { font-size: 24rpx; color: #999;
  font-weight: bold; background: rgba(255,255,255,0.6); padding: 4rpx 12rpx; border-radius: 20rpx; }

@keyframes pulse-slow {
  0% { transform: scale(1);
}
  50% { transform: scale(1.01); }
  100% { transform: scale(1); }
}

.task-note-container {
  flex-shrink: 0;
  margin-bottom: 20rpx;
}

.task-note {
  background: rgba(255, 255, 255, 0.95);
  padding: 30rpx;
  border-radius: 24rpx;
  box-shadow: 0 10rpx 30rpx rgba(255, 182, 193, 0.15);
  border: 2rpx solid #fff;
  position: relative;
}

.pin-icon {
  position: absolute;
  top: -16rpx; left: 50%; transform: translateX(-50%);
  font-size: 32rpx;
  text-shadow: 0 4rpx 4rpx rgba(0,0,0,0.1);
  filter: hue-rotate(-20deg);
}

.note-header { display: flex; align-items: center; margin-bottom: 16rpx;
}
.note-icon { 
  font-size: 44rpx; margin-right: 20rpx; 
  background: #fff0f5; padding: 10rpx; border-radius: 50%;
}
.header-text { display: flex; flex-direction: column;
}
.note-label { font-size: 20rpx; color: #ff8fa3; font-weight: bold; letter-spacing: 2rpx; }
.note-title { font-size: 30rpx; font-weight: 800; color: #5d4037; margin-top: 4rpx;
}
.note-desc { font-size: 26rpx; color: #8d6e63; line-height: 1.5; margin-bottom: 16rpx; }

.remote-tip-box {
  background: #f0f7ff; padding: 10rpx 16rpx; border-radius: 12rpx;
  font-size: 22rpx; color: #8daac7; margin-bottom: 16rpx;
  border: 1rpx dashed #dbe9fe;
}

.note-footer { display: flex; justify-content: space-between; font-size: 22rpx; color: #d7ccc8;
}
.difficulty-tag { background: #ffe4e8; color: #ff6b81; padding: 4rpx 16rpx; border-radius: 20rpx;
  font-weight: bold;}

/* === 中部：拍立得 === */
.polaroid-frame {
  background: #fff;
  /* 🟢 修改：底部 padding 从 80rpx 减少到 50rpx */
  padding: 24rpx 24rpx 50rpx 24rpx;
  box-shadow: 0 20rpx 50rpx rgba(139, 69, 19, 0.08); 
  border-radius: 6rpx;
  position: relative;
  flex: 1; 
  max-height: 880rpx; 
  margin: 10rpx 0;
  display: flex; flex-direction: column;
  transform: rotate(2deg);
  z-index: 5;
}

.polaroid-frame::after {
  content: "";
  position: absolute; top: -10rpx; left: 50%;
  transform: translateX(-50%);
  width: 100rpx; height: 28rpx; background: rgba(255, 228, 225, 0.8);
  box-shadow: 0 2rpx 4rpx rgba(0,0,0,0.05); border-radius: 2rpx;
}

.floating-updown { animation: float-tilted 5s ease-in-out infinite; }
@keyframes float-tilted {
  0%, 100% { transform: rotate(2deg) translateY(0);
}
  50% { transform: rotate(2deg) translateY(-8rpx); }
}

/* 照片区域 */
.photo-area {
  width: 100%; 
  flex: 1;
  background: #fffaf0;
  overflow: hidden;
  position: relative;
  border-radius: 2rpx;
}

.the-photo { width: 100%; height: 100%; object-fit: cover;
}

/* 空状态容器 */
.empty-state-cover { 
  width: 100%;
  height: 100%;
  position: relative; background-color: #f0f0f0; 
  overflow: hidden;
  /* 防止 swiper 溢出圆角 */
}

/* Swiper 样式 */
.style-swiper {
  width: 100%;
  height: 100%;
  padding-top: 20rpx;
  /* 给顶部留点呼吸感 */
  box-sizing: border-box;
}
.style-swiper-item {
  display: flex;
  justify-content: center;
  align-items: flex-start;
  /* 顶部对齐，或者 center */
  height: 100%;
  overflow: visible; /* 允许阴影溢出 */
}

.swiper-card {
  width: 92%;
  /* 卡片宽度，留出缝隙 */
  height: 85%; /* 卡片高度 */
  position: relative;
  border-radius: 16rpx;
  overflow: hidden;
  transform: scale(0.9);
  /* 默认缩小 */
  transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1); /* 平滑过渡 */
  box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.1);
  background: #fff;
  opacity: 0.8; /* 非选中态稍微透明 */
}

/* 选中态：放大、不透明、阴影加深 */
.swiper-card.active {
  transform: scale(1);
  opacity: 1;
  box-shadow: 0 10rpx 30rpx rgba(0,0,0,0.2);
  z-index: 10;
  border: 4rpx solid #fff; /* 增加白边，像照片 */
}

.sample-img { 
  width: 100%; height: 100%; 
  object-fit: cover; 
  opacity: 1;
}

/* 非选中态的遮罩层 */
.card-mask {
  position: absolute; top: 0; left: 0; width: 100%; height: 100%;
  background: rgba(255,255,255,0.4);
  /* 盖一层白雾 */
  pointer-events: none;
}

/* 内置风格标签 */
.inner-style-tag {
  position: absolute;
  top: 20rpx; left: 20rpx;
  background: rgba(255, 255, 255, 0.9);
  color: #5d4037;
  font-size: 22rpx;
  font-weight: bold;
  padding: 8rpx 20rpx;
  border-radius: 30rpx;
  z-index: 20;
  box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.1);
  display: flex; align-items: center;
}

.vip-badge {
  background: linear-gradient(135deg, #ffd700, #ffb900);
  color: #fff;
  font-size: 16rpx;
  padding: 2rpx 8rpx;
  border-radius: 8rpx;
  margin-left: 8rpx;
  text-shadow: 0 1rpx 1rpx rgba(0,0,0,0.1);
}

/* 底部滑动提示 */
.swipe-hint-text {
  position: absolute;
  bottom: 160rpx; /* 在按钮上方 */
  width: 100%;
  text-align: center;
  font-size: 20rpx;
  color: rgba(255,255,255,0.8);
  text-shadow: 0 2rpx 4rpx rgba(0,0,0,0.3);
  pointer-events: none;
  letter-spacing: 2rpx;
}

/* 遮罩层点击穿透 */
.empty-overlay {
  position: absolute; top: 0; left: 0; width: 100%; height: 100%;
  background: transparent;
  display: flex;
  flex-direction: column; align-items: center; justify-content: center;
  z-index: 5;
  /* 关键：允许点击穿透，以便 Swiper 可以滑动 */
  pointer-events: none;
}

/* 按钮 */
.camera-btn-circle {
  width: 120rpx; height: 120rpx; 
  background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
  border-radius: 50%; display: flex;
  align-items: center; justify-content: center;
  box-shadow: 0 8rpx 30rpx rgba(255, 154, 158, 0.4); 
  margin-bottom: 20rpx; position: relative;
  animation: pulse-strong 2s infinite;
  overflow: visible; 
  /* 关键：按钮本身恢复点击 */
  pointer-events: auto; 
}

/* VIP 按钮专属样式 */
.camera-btn-circle.vip-btn {
  background: linear-gradient(135deg, #f6d365 0%, #fda085 100%);
  box-shadow: 0 8rpx 30rpx rgba(253, 160, 133, 0.6);
}

.btn-ripple {
  position: absolute; top: -10rpx; left: -10rpx; right: -10rpx;
  bottom: -10rpx;
  border: 4rpx solid #ffb6c1;
  border-radius: 50%;
  opacity: 0; animation: ripple 2s infinite; z-index: 1;
}

/* VIP 波纹 */
.vip-ripple {
  position: absolute; top: -10rpx; left: -10rpx; right: -10rpx; bottom: -10rpx;
  border: 4rpx solid #ffd700;
  border-radius: 50%;
  opacity: 0; animation: ripple 2s infinite; z-index: 1;
}

.camera-icon-img { width: 60rpx;
  height: 60rpx; z-index: 10;
}

/* VIP 皇冠动效 */
.vip-crown {
  position: absolute;
  top: -20rpx;
  right: -10rpx;
  font-size: 32rpx;
  transform: rotate(15deg);
  z-index: 20;
  text-shadow: 0 2rpx 4rpx rgba(0,0,0,0.2);
  animation: float-crown 2s infinite ease-in-out;
}

@keyframes float-crown {
  0%, 100% { transform: rotate(15deg) translateY(0);
}
  50% { transform: rotate(15deg) translateY(-6rpx); }
}

.empty-hint { color: #fff; font-size: 26rpx; letter-spacing: 4rpx; font-weight: bold;
  text-shadow: 0 2rpx 8rpx rgba(0,0,0,0.5);
}

.quota-hint {
  color: #fff;
  font-size: 20rpx; 
  margin-top: 10rpx;
  background: rgba(0,0,0,0.3);
  padding: 4rpx 12rpx;
  border-radius: 20rpx;
  text-shadow: 0 1rpx 2rpx rgba(0,0,0,0.2);
}

/* VIP 额度样式 */
.quota-hint.vip-quota {
  background: rgba(255, 215, 0, 0.4);
  border: 1rpx solid rgba(255, 255, 255, 0.6);
  color: #fff;
  font-weight: bold;
  text-shadow: 0 1rpx 4rpx rgba(0,0,0,0.3);
}

/* 印章 */
.score-stamp {
  position: absolute; top: 20rpx; right: 20rpx;
  width: 110rpx; height: 110rpx;
  border: 4rpx solid #ff6b81;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  transform: rotate(-15deg); opacity: 0; z-index: 10;
  background: rgba(255, 255, 255, 0.8);
  box-shadow: 0 4rpx 10rpx rgba(0,0,0,0.1);
}
.stamp-circle {
  width: 90rpx; height: 90rpx;
  border: 2rpx dashed #ff6b81; border-radius: 50%;
  display: flex;
  flex-direction: column; align-items: center; justify-content: center;
}
.stamp-label { font-size: 16rpx; color: #ff6b81;
  font-weight: bold; }
.stamp-score { font-size: 36rpx; color: #ff4757;
  font-weight: 900; font-family: 'Courier New', monospace; line-height: 1;
}
.stamp-anim { animation: stamp-in 0.5s cubic-bezier(0.6, -0.28, 0.735, 0.045) forwards 0.5s;
}
@keyframes stamp-in {
  0% { transform: rotate(-15deg) scale(2);
  opacity: 0; }
  100% { transform: rotate(-15deg) scale(1); opacity: 0.9;
}
}

/* 底部手写字 */
.handwriting-caption {
  position: absolute; 
  /* 🟢 修改：配合 padding 减小，这里位置微调，使其居中于底栏 */
  bottom: 12rpx; 
  left: 0; width: 100%;
  text-align: center; font-family: 'Courier New', serif;
  color: #a1887f; font-size: 24rpx; padding: 0 30rpx;
  white-space: nowrap;
  overflow: hidden; text-overflow: ellipsis;
}

/* === 底部：每日一句 === */
.daily-quote-box {
  text-align: center; opacity: 0.9;
  flex-shrink: 0; height: 80rpx;
  display: flex; flex-direction: column;
  justify-content: center;
}
.quote-text {
  font-family: "PingFang SC", serif; font-size: 24rpx;
  color: #5d4037;
  letter-spacing: 2rpx;
  font-style: italic; display: block;
  margin-bottom: 6rpx;
}
.quote-author { font-size: 18rpx; color: #bcaaa4; text-transform: uppercase; letter-spacing: 4rpx;
}

/* === 底部：按钮区 === */
.action-bar {
  width: 100%; display: flex; justify-content: center; z-index: 20;
  flex-shrink: 0; height: 100rpx;
}

.replace-tip {
  position: absolute;
  top: -50rpx;
  width: 100%;
  text-align: center;
  font-size: 22rpx;
  color: #e57373;
  background: rgba(255, 255, 255, 0.9);
  padding: 6rpx 0;
  border-radius: 10rpx;
  box-shadow: 0 2rpx 10rpx rgba(0,0,0,0.05);
}

.confirm-actions { display: flex; width: 100%; justify-content: center; align-items: center;
  gap: 40rpx; }
.action-btn-wrapper { display: flex; flex-direction: column; align-items: center; }
.btn-circle {
  width: 80rpx; height: 80rpx; border-radius: 50%;
  background: #fff;
  box-shadow: 0 6rpx 20rpx rgba(139, 69, 19, 0.1);
  display: flex; align-items: center; justify-content: center; margin-bottom: 10rpx;
  color: #8d6e63;
}
.btn-label { font-size: 20rpx; color: #8d6e63; }
.btn-confirm-main {
  background: #5d4037; color: #fff; border-radius: 50rpx; padding: 0 60rpx;
  height: 80rpx;
  line-height: 80rpx; font-size: 28rpx;
  box-shadow: 0 10rpx 20rpx rgba(93, 64, 55, 0.25);
}

.placeholder-box { 
  /* 🟢 修改：从 80rpx 减小到 20rpx */
  height: 20rpx; 
  width: 100%; 
  flex-shrink: 0;
}

/* 动画与 Loading */
@keyframes pulse-strong { 
  0% { transform: scale(1); box-shadow: 0 8rpx 30rpx rgba(255, 105, 180, 0.4);
} 
  50% { transform: scale(1.05); box-shadow: 0 12rpx 40rpx rgba(255, 105, 180, 0.6);
} 
  100% { transform: scale(1); box-shadow: 0 8rpx 30rpx rgba(255, 105, 180, 0.4);
} 
}
@keyframes ripple { 
  0% { transform: scale(1); opacity: 0.8; } 
  100% { transform: scale(1.5);
  opacity: 0; } 
}
@keyframes spin { 
  0% { transform: rotate(0deg); } 
  100% { transform: rotate(360deg);
} 
}
.slide-in-down { animation: slideInDown 0.8s ease-out; }
.slide-in-up { animation: slideInUp 0.5s ease-out;
}
@keyframes slideInDown { 
  from { transform: translateY(-30rpx); opacity: 0; } 
  to { transform: translateY(0); opacity: 1;
} 
}
@keyframes slideInUp { 
  from { transform: translateY(30rpx); opacity: 0; } 
  to { transform: translateY(0);
  opacity: 1; } 
}

.loading-mask { 
  position: absolute; top: 0; left: 0; width: 100%; height: 100%;
  background: rgba(255, 255, 255, 0.85); backdrop-filter: blur(4px); 
  display: flex; flex-direction: column; align-items: center; justify-content: center; z-index: 20;
}
.loading-spinner { 
  width: 50rpx; height: 50rpx; border: 6rpx solid #ffd1dc; border-top: 6rpx solid #ff6b81; border-radius: 50%;
  animation: spin 1s linear infinite; margin-bottom: 20rpx; 
}
.loading-text { font-size: 24rpx; color: #ff6b81; font-weight: bold; letter-spacing: 2rpx;
}

/* 广告替代 Modal 样式 */
.ad-modal-mask {
  position: fixed; 
  top: 0; 
  left: 0; 
  width: 100%; 
  height: 100%;
  background: rgba(0, 0, 0, 0.9); /* 深邃的背景 */
  display: flex; 
  flex-direction: column; 
  align-items: center; 
  justify-content: center;
  z-index: 9999;
  /* 确保在最上层 */
  animation: fadeIn 0.5s ease;
  backdrop-filter: blur(10px); /* 增加模糊感 */
}

.ad-modal-content {
  display: flex; 
  flex-direction: column;
  align-items: center;
}

.ad-modal-emoji {
  font-size: 80rpx;
  margin-bottom: 60rpx;
  animation: float-tilted 3s infinite ease-in-out;
}

.ad-modal-text {
  color: #fff;
  font-size: 34rpx; 
  font-weight: 300; /* 纤细字体更显高级 */
  margin-bottom: 80rpx; 
  letter-spacing: 4rpx;
  text-align: center; 
  opacity: 0.9;
}

.ad-countdown {
  color: #ff6b81; 
  font-size: 120rpx; 
  font-weight: 100; /* 极细字体 */
  font-family: -apple-system, 'Helvetica Neue', sans-serif;
  animation: pulse-count 1s infinite;
}

@keyframes pulse-count {
  0% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.1);
  opacity: 0.7; }
  100% { transform: scale(1); opacity: 1; }
}

@keyframes fadeIn { from { opacity: 0;
} to { opacity: 1; } }

/* 新增：已保存状态的按钮样式 */
.btn-confirm-main.saved-state {
  background: #a5d6a7; /* 柔和的绿色，表示成功 */
  box-shadow: none;
  opacity: 0.9;
  transform: scale(0.98); /* 稍微缩小一点，表示按下的状态 */
}

/* === 📸 新增：拍照确认引导相关样式 === */
.guide-modal-container {
  position: fixed;
  top: 0; left: 0; width: 100%; height: 100%;
  z-index: 10000;
  background: #000;
  display: flex;
  flex-direction: column;
}

.preview-bg {
  width: 100%;
  height: 100%;
  position: absolute;
  top: 0; left: 0;
  z-index: 1;
}

.guide-overlay {
  position: relative;
  width: 100%;
  height: 100%;
  z-index: 5;
  pointer-events: none; /* 让点击穿透到底部按钮 */
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

/* 引导框：利用 box-shadow 实现中间镂空遮罩 */
.guide-box {
  width: 80%;
  height: 60%; /* 竖向比例 */
  border-radius: 40% 40% 50% 50%; /* 模拟头部轮廓 */
  box-shadow: 0 0 0 2000px rgba(0, 0, 0, 0.7); /* 超大阴影作为背景遮罩 */
  border: 4rpx dashed rgba(255, 255, 255, 0.6);
  position: relative;
  margin-bottom: 40rpx;
}

/* 四角装饰 */
.corner {
  position: absolute;
  width: 40rpx; height: 40rpx;
  border-color: #ff6b81;
  border-style: solid;
}
.c-tl { top: -4rpx; left: -4rpx; border-width: 8rpx 0 0 8rpx; border-top-left-radius: 20rpx; }
.c-tr { top: -4rpx; right: -4rpx; border-width: 8rpx 8rpx 0 0; border-top-right-radius: 20rpx; }
.c-bl { bottom: -4rpx; left: -4rpx; border-width: 0 0 8rpx 8rpx; border-bottom-left-radius: 20rpx; }
.c-br { bottom: -4rpx; right: -4rpx; border-width: 0 8rpx 8rpx 0; border-bottom-right-radius: 20rpx; }

.guide-tip-text {
  display: flex; flex-direction: column;
  align-items: center;
  color: #fff;
  font-size: 32rpx;
  font-weight: bold;
  letter-spacing: 2rpx;
  text-shadow: 0 2rpx 8rpx rgba(0,0,0,0.8);
  margin-top: 20rpx;
}

/* 底部按钮栏 */
.guide-action-bar {
  position: absolute;
  bottom: 0; left: 0; width: 100%;
  height: 180rpx;
  z-index: 10;
  display: flex;
  justify-content: space-around;
  align-items: center;
  background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
  padding-bottom: 40rpx;
  box-sizing: border-box;
}

.guide-btn {
  padding: 20rpx 60rpx;
  border-radius: 50rpx;
  font-size: 30rpx;
  font-weight: bold;
  letter-spacing: 2rpx;
}

.guide-btn.retake {
  background: rgba(255, 255, 255, 0.2);
  color: #fff;
  border: 2rpx solid rgba(255, 255, 255, 0.4);
  backdrop-filter: blur(5px);
}

.guide-btn.confirm {
  background: #ff6b81;
  color: #fff;
  box-shadow: 0 8rpx 20rpx rgba(255, 107, 129, 0.4);
}