/* miniprogram/pages/playground/index.wxss */
page {
  background: #fdfbf7;
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', sans-serif;
}

.playground-container {
  padding: 40rpx;
}

/* 复用纸质纹理 */
.paper-texture {
  background: #fff;
  border-radius: 24rpx;
  box-shadow: 0 8rpx 24rpx rgba(149, 157, 165, 0.08);
  position: relative;
}

/* === 花园卡片 === */
.garden-card {
  padding: 30rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 40rpx;
  border: 2rpx dashed #aed9e9; /* 淡蓝色边框 */
  background: linear-gradient(180deg, #f0f8ff 0%, #fff 100%);
}

.card-header {
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.card-title { font-size: 30rpx; font-weight: 800; color: #5d4037; }

/* 水滴徽章 */
.water-badge {
  background: #e1f5fe; color: #0288d1;
  font-size: 22rpx; padding: 6rpx 16rpx;
  border-radius: 20rpx; font-weight: bold;
  flex-shrink: 0;
}

/* 收获徽章 */
.harvest-badge {
  background: #fff8e1; color: #ff8f00;
  font-size: 22rpx; padding: 6rpx 16rpx;
  border-radius: 20rpx; font-weight: bold;
  border: 1rpx solid #ffe082;
  flex-shrink: 0;
}

.water-info-sub {
  width: 100%; text-align: right;
  font-size: 20rpx; color: #0288d1;
  margin-top: -10rpx; margin-bottom: 20rpx;
}

.plant-stage {
  height: 240rpx; /* 稍微调高一点，给花朵更多空间 */
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 20rpx;
}

.flower-img { width: 220rpx; height: 220rpx; }

/* 呼吸动画 */
.bounce-anim { animation: bounce 3s infinite ease-in-out; }
.glow-anim {
  filter: drop-shadow(0 0 16rpx rgba(255, 105, 180, 0.6));
  animation: float 3s infinite ease-in-out;
}

@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-12rpx); }
}
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-16rpx); }
}

/* 进度条 */
.progress-box { width: 100%; margin-bottom: 40rpx; }
.progress-bar {
  height: 16rpx; background: #e0e0e0; border-radius: 10rpx; overflow: hidden;
  margin-bottom: 10rpx;
}
.progress-inner {
  height: 100%; 
  background: #ff6b81;
  border-radius: 10rpx;
  transition: width 0.5s ease;
}
.level-text { font-size: 22rpx; color: #8d6e63; display: block; text-align: center; font-weight: 500;}
.max-level { color: #ff6f00; font-weight: bold; }

/* === 按钮样式优化 (修复崩坏问题) === */
.btn-water {
  background: #ff6b81; 
  color: #fff;
  border-radius: 44rpx; 
  font-size: 28rpx;
  
  /* 🔴 修复核心：固定宽高和 flex 居中 */
  width: 60% !important;
  height: 88rpx;
  line-height: 88rpx;
  padding: 0;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: center;
  white-space: nowrap; /* 防止换行 */
  
  box-shadow: 0 6rpx 16rpx rgba(255, 107, 129, 0.3);
}

.btn-harvest {
  background: linear-gradient(45deg, #ffca28, #ff6f00);
  color: #fff;
  border-radius: 44rpx; 
  font-size: 30rpx;
  
  /* 🔴 修复核心：固定宽高和 flex 居中 */
  width: 65% !important; /* 比浇水按钮稍微宽一点 */
  height: 96rpx;         /* 更有分量感 */
  line-height: 96rpx;
  padding: 0;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: center;
  white-space: nowrap; /* 强制不换行 */
  
  box-shadow: 0 8rpx 20rpx rgba(255, 111, 0, 0.3);
  font-weight: bold;
  animation: pulse 2s infinite;
}

@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.05); }
  100% { transform: scale(1); }
}

.tip-text { font-size: 20rpx; color: #b0bec5; margin-top: 24rpx; text-align: center; }

/* === 宫格菜单 === */
.grid-menu {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

.grid-item {
  width: 48%; /* 两列 */
  padding: 30rpx;
  box-sizing: border-box;
  margin-bottom: 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.grid-icon { font-size: 50rpx; margin-bottom: 10rpx; }
.grid-title { font-size: 26rpx; font-weight: bold; color: #5d4037; }
.grid-desc { font-size: 20rpx; color: #a1887f; margin-top: 4rpx; opacity: 0.8; }