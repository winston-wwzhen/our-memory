/* miniprogram/pages/avatar_list/index.wxss */
page {
  background: #fff0f5; 
  font-family: -apple-system, 'Helvetica Neue', sans-serif;
  min-height: 100vh;
}

.container {
  position: relative;
  min-height: 100vh;
  background: linear-gradient(180deg, #fff0f5 0%, #fffaf0 100%);
}

/* === 🌸 背景动态装饰 === */
.bg-decoration {
  position: absolute;
  top: 0; left: 0; width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 0; 
  overflow: hidden;
}

.floating-heart {
  position: absolute;
  bottom: -50rpx;
  font-size: 30rpx; color: #ffdee9;
  opacity: 0.6;
  animation: floatUp linear infinite;
  text-shadow: 0 0 10rpx rgba(255, 182, 193, 0.5);
}
.h1 { left: 10%; animation-duration: 8s; font-size: 24rpx; }
.h2 { left: 30%; animation-duration: 12s; animation-delay: 2s; font-size: 36rpx; color: #ffb7b2; }
.h3 { left: 60%; animation-duration: 10s; animation-delay: 4s; }
.h4 { left: 80%; animation-duration: 15s; animation-delay: 1s; font-size: 20rpx; }
.h5 { left: 90%; animation-duration: 9s; animation-delay: 6s; font-size: 28rpx; }

@keyframes floatUp {
  0% { transform: translateY(0) rotate(0deg); opacity: 0; }
  20% { opacity: 0.8; }
  100% { transform: translateY(-120vh) rotate(360deg); opacity: 0; }
}

/* === 内容容器 === */
.content-wrapper {
  position: relative;
  z-index: 10;
  padding-bottom: 60rpx;
}

/* === 顶部 Banner === */
.banner-box {
  /* ✨ 极限收窄：左右仅留 10rpx */
  margin: 20rpx 10rpx; 
  padding: 30rpx 40rpx; /* 内部 padding 保持舒适 */
  border-radius: 24rpx; /* 圆角稍微改小一点点以适配窄边 */
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(10px);
  border: 2rpx solid rgba(255, 255, 255, 0.8);
  box-shadow: 0 10rpx 30rpx rgba(255, 182, 193, 0.2);
}

.banner-text .title {
  font-size: 36rpx; /* 稍微调整 Banner 字体大小适配紧凑布局 */
  font-weight: 800;
  color: #5d4037;
  display: block;
  letter-spacing: 2rpx;
  margin-bottom: 6rpx;
}

.banner-text .subtitle {
  font-size: 22rpx;
  color: #ff8fa3;
  font-weight: 600;
}

/* === 瀑布流 Grid (极致紧凑双列) === */
.avatar-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  /* ✨ 间距和边距都设为 10rpx */
  gap: 10rpx;
  padding: 0 10rpx;
}

/* === 卡片样式 === */
.card {
  background: #fff;
  border-radius: 20rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 16rpx rgba(139, 69, 19, 0.04);
  transition: all 0.2s ease;
  position: relative;
  border: 1rpx solid rgba(255, 240, 245, 0.8);
}

.card-hover {
  transform: scale(0.98);
  box-shadow: 0 2rpx 8rpx rgba(139, 69, 19, 0.08);
}

.image-box {
  position: relative;
  width: 100%;
  /* ✨ 2:1 比例精确计算 */
  /* 屏幕宽 750rpx */
  /* 减去左右 padding (10*2) = 20 */
  /* 减去中间 gap (10) */
  /* 剩余空间 = 720rpx */
  /* 单卡片宽 = 360rpx */
  /* 高度 = 360 / 2 = 180rpx */
  height: 180rpx; 
  display: block;
  background-color: #f2f2f2;
}

.cover-img {
  width: 100%;
  height: 100%;
  /* 填满 2:1 容器，超出的部分裁切 */
  object-fit: cover; 
  transition: opacity 0.3s ease;
}

/* 标签 */
.tag {
  position: absolute; top: 8rpx; left: 8rpx;
  padding: 4rpx 10rpx; border-radius: 8rpx;
  font-size: 18rpx; font-weight: bold;
  color: #fff; z-index: 5;
  box-shadow: 0 2rpx 6rpx rgba(0,0,0,0.15);
}
.tag.hot { background: linear-gradient(90deg, #ff9a9e, #ff6b81); }
.tag.vip { background: linear-gradient(90deg, #ffd700, #ffb900); }

/* 信息区 */
.info-box { padding: 16rpx; }

.card-title {
  /* 卡片变宽了，字号可以稍微大一点点 */
  font-size: 28rpx; 
  font-weight: 700;
  color: #5d4037;
  margin-bottom: 8rpx;
  white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
}

.card-meta { 
  display: flex; 
  justify-content: space-between; 
  align-items: center; 
}

.count { 
  font-size: 22rpx; 
  color: #bcaaa4; 
}
.count::before {
  content: '♥';
  margin-right: 4rpx;
  color: #ffb7b2;
}

/* === 底部 Loading === */
.footer-state { padding: 40rpx 0; text-align: center; }
.loading-wrap { display: flex; justify-content: center; gap: 10rpx; }
.dot {
  width: 10rpx; height: 10rpx; background: #ffb7b2;
  border-radius: 50%; animation: bounce 1.4s infinite ease-in-out both;
}
.dot:nth-child(1) { animation-delay: -0.32s; }
.dot:nth-child(2) { animation-delay: -0.16s; }
@keyframes bounce {
  0%, 80%, 100% { transform: scale(0); }
  40% { transform: scale(1); }
}
.no-more { font-size: 22rpx; color: #d7ccc8; letter-spacing: 2rpx; }

/* 定制按钮 */
.make-btn {
  background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
  padding: 14rpx 24rpx;
  border-radius: 40rpx;
  box-shadow: 0 4rpx 12rpx rgba(255, 107, 129, 0.3);
  border: 2rpx solid rgba(255, 255, 255, 0.6);
  transition: all 0.2s ease;
}

.btn-content { display: flex; align-items: center; gap: 6rpx; }
.btn-icon { font-size: 26rpx; }
.btn-txt { font-size: 24rpx; font-weight: 700; color: #fff; letter-spacing: 1rpx; }
.btn-hover { transform: scale(0.95); filter: brightness(0.95); box-shadow: 0 2rpx 8rpx rgba(255, 107, 129, 0.2); }