# 📖 我们的纪念册 (Our Memory)

> **把平淡的日常，画成漫画里的模样。**
>
> **Record your love, paint your life.**

一款集 **AI 绘画**、**恋爱养成**、**双人互动** 于一体的微信小程序。
在这里，你们可以完成每日的趣味打卡，通过多模态 AI 将合照变成唯美的二次元画作，共同培育代表爱情的玫瑰，并用它兑换专属的心愿特权。

---

## ✨ 核心功能 (Core Features)

### 1. 📸 每日心动 (Today - AI Camera)
* **每日任务**：基于日期算法的每日挑战（如“摸头杀”、“搞怪鬼脸”），解决“今天拍什么”的烦恼。
* **AI 魔法变身**：支持 **13+ 种风格**切换（日漫、水彩、3D迪士尼、黏土风、美式热漫等）。
* **拍立得生成**：自动封装成拍立得样式，包含照片、AI 评分印章及智能点评。

### 2. 🎡 恋爱乐园 (Playground - Gamification)
这是一个闭环的“爱情经济系统”：
* **🌹 玫瑰花园**：二人共同浇灌培育玫瑰，成熟后可“收获”玫瑰作为货币。
* **🎁 心愿工坊**：消耗玫瑰制作兑换券（如：💆‍♂️揉肩卡、🍽️免洗金牌、🤝和好卡）。
* **🎲 命运抉择**：趣味翻牌玩法，解决“今天吃什么”等选择困难症。

### 3. 🎞️ 时光长廊 (Memory Lane)
* **双人时间轴**：将二人的打卡记录按时间串联，区分“ME”和“TA”的视角。
* **里程碑奖励**：记录累计打卡天数，设有 30 天惊喜彩蛋提示。

### 4. 🏠 专属空间 (Mine & Binding)
* **双人绑定**：通过“专属编号”邀请另一半，绑定后数据互通。
* **恋爱计时器**：自定义纪念日，实时计算相爱天数。

---

## 🛠️ 技术栈 (Tech Stack)

本项目完全基于 **微信小程序云开发 (WeChat Cloud Development)**。

* **前端**：微信小程序原生 (WXML, WXSS, JS)，使用了 CSS3 动画（浮动气泡、呼吸灯、翻转卡片）。
* **后端**：微信云函数 (Node.js)
    * `process_anime`: **核心**。集成了 Axios，负责图片下载、多模型 API 调用 (SiliconFlow/Tencent)、Prompt 组装及图片上传。
    * `user_center`: 用户体系、花园逻辑、积分扣减。
    * `get_daily_mission`: 每日任务算法。
* **AI 能力池**：
    * **主力**：SiliconFlow / 302.AI (FLUX.1-schnell, Kolors)
    * **兜底**：腾讯云 AI 绘画 (ImageToImage)
    * **辅助**：GPT-4o-mini (用于视觉反推 Prompt)
* **数据库**：云开发 Database (NoSQL)。

---

## 🚀 部署指南 (Deployment)

### 1. 准备工作
* 注册微信小程序账号并开通云开发。
* 注册 **腾讯云** 账号，开通 AI 绘画服务，获取 `SecretId` 和 `SecretKey`。
* 注册 **SiliconFlow (硅基流动)** 或 **302.AI** 账号，获取 API Key。

### 2. 项目初始化
1.  克隆项目到本地并导入微信开发者工具。
2.  在 `project.config.json` 中修改 `appid`。
3.  在 `miniprogram/app.js` 中替换云开发环境 ID。

### 3. 云函数配置 (关键步骤)
1.  **安装依赖**：
    * 在 `cloudfunctions/process_anime` 目录下右键打开终端，运行：
        ```bash
        npm install axios tencentcloud-sdk-nodejs wx-server-sdk
        ```
2.  **配置密钥**：
    * 复制 `cloudfunctions/process_anime/config.example.js` 为 `config.js`。
    * 填入你的多模型配置信息：
        ```javascript
        module.exports = {
          TENCENT: {
            SID: "你的腾讯云SID",
            SKEY: "你的腾讯云SKEY",
            REGION: "ap-shanghai",
          },
          SILICONFLOW: {
            KEY: "sk-你的SiliconFlow或302Key",
            URL: "[https://api.siliconflow.cn/v1/images/generations](https://api.siliconflow.cn/v1/images/generations)", // 或 302.AI 地址
          }
        };
        ```
3.  **上传部署**：
    * 右键 `process_anime` 文件夹，选择 **“上传并部署：云端安装依赖”**。
    * **注意**：建议在云开发控制台将该函数的超时时间设置为 **60秒**，以预留降级重试的时间。

### 4. 数据库初始化
1.  部署 `init_task_pool` 云函数。
2.  在云开发控制台 -> 云函数 -> `init_task_pool` -> 点击“测试”，写入基础任务数据。
3.  创建集合：`users`, `logs`, `task_pool`, `gardens`, `coupons`, `app_config`。

### 5. VIP 配置
* 在 `app_config` 集合中添加 `global_settings` 记录，配置 `sudo_users` 数组（填入管理员 OpenID）以测试 VIP 功能。

---

## 📂 目录结构 (Structure)

```text
├── cloudfunctions/
│   ├── process_anime/      # [核心] AI 绘图、多模型路由、降级重试
│   │   ├── index.js        # 业务逻辑
│   │   ├── config.js       # 密钥配置 (需新建)
│   │   └── package.json    # 依赖: axios, tencentcloud-sdk...
│   ├── user_center/        # 用户、花园、胶囊、留言板逻辑
│   ├── get_daily_mission/  # 每日任务算法
│   └── init_task_pool/     # 初始化脚本
├── miniprogram/
│   ├── pages/              # 页面 (index, playground, mine, history...)
│   ├── images/             # 静态资源
│   └── app.js              # 全局入口
└── project.config.json     # 项目配置